<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Learn-PHP</title>
</head>

<body>

</body>
<?php
$string = 'I am a String value';

// echo strtolower($string); // to Lower Case
// echo strtoupper($string); // to Upper Case
// echo trim('   I have soo many space   '); // trim excess Spaces
// echo str_pad($string, 50, '-'); // padding to right with specified char
// echo str_replace(' ', '', $string); // Classic string replace method
// echo strrev($string); // reverse a string value
// echo str_shuffle($string); // Randomly shuffle String value
// echo (strcmp($string, 'I am a String value') === 0) ? 'true' : 'false'; // compare 2 string, return 0 if true
// echo strlen($string); // length method
// echo strpos($string, ' '); // get the position of first occurence
// echo substr($string, 7, 7); // substring but the third param is length of the char after the second param

// $string = explode(' ', $string);
// foreach($string as $element) {
//     echo "{$element}<br>";
// } // String split method

// $strings = explode(' ', $string);
// $string = implode('-', $strings);
// echo $string; // Opposite of split / explode method

?>

</html>